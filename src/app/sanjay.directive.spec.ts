import { SanjayDirective } from './sanjay.directive';

describe('SanjayDirective', () => {
  it('should create an instance', () => {
    const directive = new SanjayDirective();
    expect(directive).toBeTruthy();
  });
});
